{
  "name": "salesradiant",
  "version": "1.0.0",
  "private": true,
  "description": "SalesRadiant marketing site — static page plus a lead-capture endpoint",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:standalone": "node build-preview.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
